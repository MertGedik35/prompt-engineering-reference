# Prompt Doctor

Prompt Doctor is a diagnostic index for failing prompts. Each entry includes likely
causes, a quick diagnostic, a minimal fix, a robust fix, a relevant pattern, and a
verification method.

The source records live in `catalog/prompt_doctor.json`.
