# Quizzes

The 13 authoritative quizzes live in `curriculum/<module>/quiz.md`. Each contains eight
module-specific questions and links to an explained answer key in `labs/solutions/`. Attempt the
quiz before opening its solution.
